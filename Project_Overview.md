# 羽球計分板 (Badminton Scoreboard) 專案總覽

這是一份總結目前專案所使用的**硬體架構**與**軟體功能**的完整說明文件。

---

## 1. 核心控制器與顯示器
### ESP32 開發板
*   **角色**：整個計分板的大腦，負責接收手機藍牙指令、遙控器訊號，並控制 LED 矩陣顯示與語音播放。
*   **通訊架構**：目前主要採用 **BLE (Bluetooth Low Energy)** 擔任 GATT Server，接收來自手機前端網頁的控制指令 (如 `'A'`, `'B'`, `'A-'`, `'R'`, `'SETUP'`)。

### HUB75 全彩 LED 矩陣面板 (P6)
*   **規格**：常見解析度為 64x32 或 64x64。
*   **驅動方式**：使用 `ESP32-HUB75-MatrixPanel-I2S-DMA` 函式庫，透過 ESP32 的 I2S DMA 資源進行不閃爍的高速硬體繪圖。

---

## 2. 擴充硬體模組

### A. 語音報分模組 (CX1000AC / WT2003S)
*   **功能**：在分數變更時，自動利用內建擴音器播放目前的比分（例如：「十五 比 十四」）。
*   **硬體接線**：
    *   **ESP32 TX (發送)** ➜ 接至 GPIO 39 (連接模組 RX)
    *   **ESP32 RX (接收)** ➜ 接至 GPIO 40 (連接模組 TX)
*   **通訊協定**：使用 9-byte 序列埠 Hex 指令 (`0x7E 0x09 0xFF 0xFF 0x07 [High] [Low] [Checksum] 0xEF`)。
*   **語音檔對應**：內部記憶體存放 `00000.mp3` ~ `00031.mp3`。
    *   `00000.mp3` 對應 0 分，以此類推。
    *   `00031.mp3` 對應「比」字。
*   **軟體設計**：採用非阻塞式 (Non-blocking) `std::queue<int> voiceQueue` 搭配 `millis()` 進行依序播放，避免語音播放的等待時間卡住 ESP32 主程式。

### B. 433MHz 無線遙控模組 (SYN480R + EV1527)
*   **功能**：提供實體四鍵遙控器（A、B、C、D鍵）供裁判在場邊快速加減分，支援複合鍵。
*   **硬體接線**：
    *   **DATA / OUT** ➜ 接至 ESP32 的 GPIO 4 (需支援外部中斷)。
*   **軟體設計**：
    *   使用 `rc-switch` (sui77) 函式庫進行解碼。
    *   EV1527 發送 24-bit 訊號（20-bit 內碼 + 4-bit 按鍵狀態）。
    *   目前備有獨立的 `test_433mhz.ino` 測試程式來讀取遙控器的 Decimal 代碼，未來將整合至主程式中對應加減分動作。

---

## 3. 軟體與前端網頁架構 (Web & BLE)

### 前端控制介面 (GitHub Pages)
*   **位置**：託管於 `https://isaacyang34.github.io/Homepage/Badminton_Score_BLE.html`
*   **優勢**：前後端完全分離，利用 GitHub Pages 提供的強制 HTTPS 加密環境，來滿足現代瀏覽器存取**藍牙 (Web Bluetooth API)** 與 **麥克風 (Web Speech API)** 的嚴格安全要求。

### 核心軟體功能
1.  **BLE 藍牙控制**：
    *   透過 Web Bluetooth API 搜尋特定的 Service UUID 與 Characteristic UUID，手機可直接發送字元指令給 ESP32，無需連上特定的 WiFi 熱點，減少干擾與斷線疑慮。
2.  **語音辨識控制 (Web Speech API)**：
    *   **無硬體成本**：利用手機/平板自身的麥克風與雲端語音辨識引擎。
    *   **操作**：點擊網頁上的語音按鈕後，只要說出「A隊加分」、「分數歸零」等關鍵字，JavaScript 就會自動攔截並轉化為對應的 BLE 指令 (`sendCmd('A')`) 發送給 ESP32。
3.  **進階賽程管理 (內建於網頁)**：
    *   支援單打、雙打的球員登錄。
    *   具備「一般亂數配對」與「四強錦標賽 (贏打贏、輸打輸)」的自動排程演算法。
4.  **設定模式切換**：
    *   可透過前端發送 `SETUP` 指令，讓 ESP32 關閉 BLE 並重啟進入 WiFi AP 模式，以便後續進階設定。
